import React from "react";
import { Home } from "../components";

const Main = () => {
  return <main className="w-screen min-h-screen flex items-center justify-start bg-primary">
    <Home />
  </main>
};

export default Main;
